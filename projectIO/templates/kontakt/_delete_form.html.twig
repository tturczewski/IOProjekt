<form method="post" action="{{ path('kontakt_delete', {'id': kontakt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kontakt.id) }}">
    <button class="btn">Delete</button>
</form>
