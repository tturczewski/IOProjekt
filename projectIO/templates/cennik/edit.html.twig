{% extends 'base.html.twig' %}

{% block title %}Edit Cennik{% endblock %}

{% block body %}
    <h1>Edit Cennik</h1>

    {{ include('cennik/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cennik_index') }}">back to list</a>

    {{ include('cennik/_delete_form.html.twig') }}
{% endblock %}
